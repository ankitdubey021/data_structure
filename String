#include<stdio.h>
#include<conio.h>

int stlen(char *str){
   int l=0;
   while(*str!=0){
     l++;
     str++;
   }
   return l;
}

void main(){
 char ch[]="abc";
 int len;
 clrscr();
 len=stlen(ch);
 printf("Total character found : %d",len);
 getch();
}
